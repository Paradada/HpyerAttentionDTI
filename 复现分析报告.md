# HyperAttentionDTI 复现静态分析报告

> 分析对象：`d:\code\HpyerAttentionDTI`
> 论文：Zhao et al., "HyperAttentionDTI: improving drug–protein interaction prediction by sequence-based deep learning with attention mechanism", Bioinformatics, 2022, 38(3):655–662.
> 分析方式：纯静态代码分析（未运行、未修改代码）。所有结论均给出 `文件路径 + 类/函数名 + 行号 + 代码逻辑`。

---

## 0. 一句话结论

- **模型本体（Embedding + CNN + HyperAttention + FC 分类器）基本实现**，其中 HyperAttention 的核心「drug位置 × protein位置 × feature channel」交互**已实现**。
- **但存在多处与论文数值不一致**：CNN 通道基数（40 vs 32）、蛋白 kernel（[4,8,12] vs [4,6,12]）、学习率（5e-5 vs 1e-4）、batch size（4 vs 32）、early stopping patience（50 vs 20）、损失函数（CrossEntropyLoss vs BCE）。
- **四种实验 E1–E4 只实现了 E1（随机 pair-level 划分），E2/E3/E4 完全没有实现**，Table 8（消融）也没有。
- 因此：**当前代码只能"跑一个 HyperAttentionDTI 模型"，无法直接得到论文 Table 2–8 的复现结果**。

---

## 1. 项目结构

### 1.1 文件清单

| 文件 | 作用 |
|---|---|
| `HpyerAttentionDTI/HyperAttentionDTI_main.py` | **入口 / 训练 / 测试 / 指标统计** |
| `HpyerAttentionDTI/model.py` | **模型定义**（`AttentionDTI` 类，含 CNN、HyperAttention、FC） |
| `HpyerAttentionDTI/dataset.py` | 数据解析（字符表、SMILES/蛋白转索引、`collate_fn`） |
| `HpyerAttentionDTI/hyperparameter.py` | 超参数（`hyperparameter` 类） |
| `HpyerAttentionDTI/pytorchtools.py` | 早停（`EarlyStopping` 类） |
| `HpyerAttentionDTI/data/DrugBank.txt` | DrugBank 数据（35022 行） |
| `HpyerAttentionDTI/data/Davis.txt` | Davis 数据（25772 行） |
| `HpyerAttentionDTI/data/KIBA.txt` | KIBA 数据（116350 行） |
| `HpyerAttentionDTI/README.md` | 官方说明（含运行命令） |
| `HpyerAttentionDTI/requirements.txt` | 依赖 |
| `HpyerAttentionDTI/Linux安装说明.md` / `预实验记录与代码说明.md` / `代码说明.txt` | 环境与预实验记录（**非代码**） |

### 1.2 关键结论

- **入口文件**：`HyperAttentionDTI_main.py`（`if __name__ == "__main__"` 位于第 140 行）。
- **数据文件**：`data/` 下三个 `.txt`，**已随仓库提供、已二值化好**（label 已是 0/1）。
- **训练/测试文件**：没有独立的 test 脚本，训练和测试都在同一个入口文件里。
- **E2/E3/E4、消融实验**：**没有任何对应脚本**。

---

## 2. 论文实验与代码对应表

| 论文实验 | 数据集 | 论文 Setting | 代码位置 | 是否实现 |
|---|---|---|---|---|
| Table 2 | DrugBank | E1 (16:4:5) | `HyperAttentionDTI_main.py:140-359`（5 折随机 pair-level 划分） | ⚠️ 部分（随机划分，非显式 E1 脚本；比例恰为 64/16/20≈16:4:5） |
| Table 3 | Davis | E1 | 同上，改 `DATASET="Davis"` | ⚠️ 部分 |
| Table 4 | KIBA | E1 | 同上，改 `DATASET="KIBA"` | ⚠️ 部分 |
| Table 5 | DrugBank | E2（novel drug） | **无** | ❌ 未实现 |
| Table 6 | DrugBank | E3（novel protein） | **无** | ❌ 未实现 |
| Table 7 | DrugBank | E4（novel drug+protein） | **无** | ❌ 未实现 |
| Table 8 | 消融 | 去掉/替换 attention | **无** | ❌ 未实现 |

> 说明：E1 在论文中的定义是「seen drug + seen protein + unseen pair」，本质就是**随机 pair-level 划分**，与当前代码的 5 折随机划分语义一致。因此「E1 类型」的实验当前代码可以跑；但论文中 E1 用于 Table 2–4 的**数值结果无法直接复现**，因为模型超参数与统计方式均不一致（见 §4）。

---

## 3. 数据集的代码对应分析

### 3.1 数据文件位置与格式

- 文件：`HpyerAttentionDTI/data/DrugBank.txt`、`Davis.txt`、`KIBA.txt`。
- 每行 **5 列（空格分隔）**：`Drug_ID  Protein_ID  SMILES  Amino_acid_sequence  label`
  - 示例（DrugBank.txt 第 1 行）：`DB00303 P45059 [H][C@]12[C@@H]... MVKFNSSRKS... 1`
- 实测规模（`wc -l` + 末列统计）：

| 数据集 | 总行数 | 正样本(1) | 负样本(0) | unique drug | unique protein |
|---|---|---|---|---|---|
| DrugBank | 35022 | 17511 | 17511 | 6647 | 4294 |
| Davis | 25772 | 7320 | 18452 | 68 | 379 |
| KIBA | 116350 | 22154 | 94196 | 2068 | 225 |

- **与论文一致性**：正负样本数、蛋白数**完全一致**；Davis/KIBA 的 drug 数一致；DrugBank 的 drug 数代码为 **6647，论文为 6655（差 8）**，属数据版本差异，需注意。

### 3.2 数据读取代码

- `HyperAttentionDTI_main.py:160-180`：根据 `DATASET` 变量读入对应 `.txt`，`read().strip().split('\n')` 得到行列表 `train_data_list`。
- 真正解析每行 5 列在 **`dataset.py` 的 `collate_fn`（第 46-68 行）**：

```python
# dataset.py:57
drug_id, protein_id, compoundstr, proteinstr, label = \
    pair[-5], pair[-4], pair[-3], pair[-2], pair[-1]
```

### 3.3 drug / protein / label 的表示

- **drug 表示**：SMILES 字符串 → 字符 ID。
  - 字符表 `CHARISOSMISET`（`dataset.py:5-12`），共 64 个 SMILES 字符 → 整数 ID。
  - `label_smiles()`（`dataset.py:23-27`）把 SMILES 截断/填充到 `MAX_SMI_LEN=100`。
- **protein 表示**：氨基酸序列 → 字符 ID。
  - 字符表 `CHARPROTSET`（`dataset.py:16-19`），共 25 个氨基酸字母 → 整数 ID。
  - `label_sequence()`（`dataset.py:30-34`）截断/填充到 `MAX_SEQ_LEN=1000`。
- **label 生成**：`dataset.py:64-66`，`label = float(label)` 后 `int(label)`，直接取文件第 5 列的 0/1。
  - **关键：label 不是在代码里根据阈值生成的。** Davis(5.0) / KIBA(12.1) 的阈值二值化已经**离线做进数据文件**了，代码中**没有任何阈值逻辑**（已用 grep 确认无 `threshold/5.0/12.1`）。

### 3.4 positive/negative 划分

- 划分**已经在数据文件中完成**（每行 label 已定），代码不做任何正负采样或平衡。
- DrugBank 正负 1:1 平衡 ✓；Davis/KIBA 本身不平衡，代码通过 `CrossEntropyLoss(weight=...)` 加类别权重（见 §5.3），**并非重采样**。

### 3.5 与论文一致性小结（数据部分）

| 检查项 | 论文 | 代码 | 一致 |
|---|---|---|---|
| DrugBank 样本数/正负 | 35022 / 17511 / 17511 | 一致 | ✅ |
| Davis 样本数/正负 | 25772 / 7320 / 18452 | 一致 | ✅ |
| KIBA 样本数/正负 | 116350 / 22154 / 94196 | 一致 | ✅ |
| DrugBank protein 数 | 4294 | 4294 | ✅ |
| DrugBank drug 数 | 6655 | 6647 | ⚠️ 差 8 |
| Davis 阈值 5.0 / KIBA 阈值 12.1 | 论文据此二值化 | 数据已预二值化，代码无阈值 | ⚠️ 离线完成 |
| SMILES max len / 蛋白 max len | 100 / 1000 | 100 / 1000 | ✅ |

---

## 4. 模型结构逐层对照

模型类：`model.py` 中的 `AttentionDTI`（第 13 行）。前向传播：`forward()`（第 60-94 行）。

### 4.1 Embedding layer

- `model.py:25`：`protein_embed = nn.Embedding(26, 64, padding_idx=0)`（25 氨基酸 + pad）。
- `model.py:26`：`drug_embed = nn.Embedding(65, 64, padding_idx=0)`（64 SMILES 字符 + pad）。
- embedding 维度 64 = `hp.char_dim`（`hyperparameter.py:27`）= `hp.embed_dim`（`hyperparameter.py:22`），与论文 embedding size=64 **一致** ✅。

### 4.2 CNN Block

- **drug CNN**：`model.py:27-34` `Drug_CNNs`（`nn.Sequential` 3 层 Conv1d + ReLU）。
- **protein CNN**：`model.py:36-43` `Protein_CNNs`（3 层 Conv1d + ReLU）。
- 通道数由 `hp.conv` 决定（`hyperparameter.py:26` `conv = 40`）：三层通道为 `conv → conv*2 → conv*4` = **40 → 80 → 160**。
- kernel 由 `hp.drug_kernel` / `hp.protein_kernel` 决定（`hyperparameter.py:25` / `:24`）。

| 检查项 | 论文 | 代码 | 一致 |
|---|---|---|---|
| drug CNN 3 层 | 3 层 | 3 层 | ✅ |
| protein CNN 3 层 | 3 层 | 3 层 | ✅ |
| filter 数量 | **32 / 64 / 96** | **40 / 80 / 160**（conv=40） | ❌ 通道基数 40 ≠ 32 |
| drug kernel size | 4 / 6 / 8 | `drug_kernel=[4,6,8]` | ✅ |
| protein kernel size | **4 / 6 / 12** | `protein_kernel=[4,8,12]` | ❌ 第 2 层 8 ≠ 6 |
| 激活 | — | ReLU（Conv 间） | — |

**输出 shape（`forward` 内，第 63-67 行）**：

- drug：`[B,64,100]` → 长度 100→97→92→**85**，通道 64→40→80→**160** → **`[B,160,85]`**。
- protein：`[B,64,1000]` → 长度 1000→997→990→**979**，通道 64→40→80→**160** → **`[B,160,979]`**。

（若按论文 protein kernel=[4,6,12]，长度应为 1000→997→992→981 = 981，而代码是 979，二者不一致。）

### 4.3 HyperAttention（重点）

代码位置：`model.py:69-81`。

```python
# model.py:69-70  对 drug/protein 的 feature map 做线性映射（各自到 attention 表示）
drug_att    = self.drug_attention_layer(drugConv.permute(0,2,1))      # [B, 85, 160]
protein_att = self.protein_attention_layer(proteinConv.permute(0,2,1)) # [B, 979, 160]

# model.py:72-73  构造 drug 位置 × protein 位置 的两两组合
d_att_layers = drug_att.unsqueeze(2).repeat(1,1,proteinConv.shape[-1],1)   # [B, 85, 979, 160]
p_att_layers = protein_att.unsqueeze(1).repeat(1,drugConv.shape[-1],1,1)   # [B, 85, 979, 160]

# model.py:74     结合并映射，得到 pairwise attention 表示
Atten_matrix = self.attention_layer(self.relu(d_att_layers + p_att_layers)) # [B, 85, 979, 160]

# model.py:75-78  沿 protein / drug 维度取平均，再 sigmoid 成权重
Compound_atte = self.sigmoid(torch.mean(Atten_matrix, 2).permute(0,2,1))  # [B,160,85]
Protein_atte  = self.sigmoid(torch.mean(Atten_matrix, 1).permute(0,2,1))  # [B,160,979]

# model.py:80-81  对原始 CNN feature map 重新加权
drugConv    = drugConv * 0.5 + drugConv * Compound_atte     # = drugConv*(0.5+attention)
proteinConv = proteinConv * 0.5 + proteinConv * Protein_atte
```

结论（逐条回答论文问题）：

1. **attention 计算位置**：`model.py:69-78`，相关层定义在 `model.py:45-47`
   - `attention_layer = Linear(conv*4, conv*4)`（160→160，第 45 行）
   - `protein_attention_layer = Linear(160,160)`（第 46 行）
   - `drug_attention_layer = Linear(160,160)`（第 47 行）
2. **输入 tensor**：`drugConv [B,160,85]`、`proteinConv [B,160,979]`（经 permute 后做线性映射）。
3. **输出 attention shape**：`Atten_matrix` 为 **`[B, 85, 979, 160]`**（batch × drug位置 × protein位置 × channel）。
4. **是否实现「drug位置 × protein位置 × feature channel」交互**：**✅ 已实现**。`d_att_layers + p_att_layers` 逐位置相加后经 `attention_layer` 映射，正是论文描述的三维交互；随后分别对 protein 维 / drug 维 `torch.mean` 得到各自的一维加权向量。
5. 加权方式 `drugConv*0.5 + drugConv*attention` 等价于论文 `Drug_new = Drug_CNN*(0.5 + Drug_attention)` ✅。

### 4.4 Output Block

代码位置：`model.py:35,44`（MaxPool）、`model.py:83-93`（池化+FC）。

- **Global Max Pooling**：`Drug_max_pool = MaxPool1d(85)`（`model.py:35`）、`Protein_max_pool = MaxPool1d(979)`（`model.py:44`），kernel 尺寸 = 卷积后剩余长度，效果即全局最大池化 → `squeeze(2)` 得到 drug `[B,160]`、protein `[B,160]`。
- **拼接**：`pair = torch.cat([drugConv, proteinConv], dim=1)`（`model.py:86`）→ `[B,320]`。
- **FCNN**：`model.py:55-58`
  - `fc1 = Linear(320, 1024)` → `fc2 = Linear(1024,1024)` → `fc3 = Linear(1024,512)` → `out = Linear(512,2)`。
  - 论文 FC 层：1024 / 1024 / 512 / 2 ✅。
- **激活**：`leaky_relu = nn.LeakyReLU()`（`model.py:54`）。`nn.LeakyReLU()` 默认 `negative_slope=0.01`，与论文 LeakyReLU negative slope=0.01 **一致** ✅。
- **Dropout**：`model.py:48-50` 三处 `nn.Dropout(0.1)`，论文 dropout=0.1 **一致** ✅。
  - ⚠️ 注意：`hyperparameter.py:16` 的 `FC_Dropout=0.5` 是**死参数**，`model.py` 里从未引用，实际生效 dropout 是硬编码的 0.1。
- **Loss**：`HyperAttentionDTI_main.py:227` 使用 `nn.CrossEntropyLoss(weight=weight_CE)`。论文为 **Binary Cross Entropy**，**不一致** ❌（CrossEntropyLoss = softmax + NLL，语义上二分类数值接近但实现不同，且代码还额外引入了类别权重）。

---

## 5. 训练参数对照

| 参数 | 论文 | 代码 | 是否一致 |
|---|---|---|---|
| Optimizer | AdamW | `optim.AdamW(...)`（main.py:222-223） | ✅ |
| Learning rate | **1e-4** | `Learning_rate=5e-5`（hyperparameter.py:11） | ❌ |
| Weight decay | 1e-4 | `weight_decay=1e-4`（hyperparameter.py:21；bias 组 wd=0） | ✅ |
| Embedding size | 64 | `char_dim=64`/`embed_dim=64`（hyperparameter.py:22,27） | ✅ |
| Batch size | **32** | `Batch_size=4`（hyperparameter.py:13，注释写 32） | ❌（当前被改小） |
| Dropout | 0.1 | model.py 三处 0.1 | ✅（FC_Dropout=0.5 为死参数） |
| Early stopping | **validation loss 连续 20 epoch 不降停止** | `Patience=50`（hyperparameter.py:15） | ❌（50 ≠ 20，且见下方 bug） |
| Loss | Binary Cross Entropy | `CrossEntropyLoss`（main.py:227） | ❌ |
| Epoch | （论文未明确，代码注释 200） | `Epoch=3`（hyperparameter.py:12，注释 200） | ⚠️ 当前临时值 |

**额外实现、论文参数清单未提及的项**：

- `CyclicLR` 学习率调度（main.py:225-226，base=lr，max=lr*10）。
- Xavier 初始化（main.py:210-212）。
- 类别权重 `weight_CE`：Davis=`[0.3,0.7]`（main.py:168）、KIBA=`[0.2,0.8]`（main.py:175）、DrugBank=`None`。

**两个需要重点指出的训练流程 bug/不一致**：

1. **早停从未真正生效**：`EarlyStopping.__call__`（pytorchtools.py:28-43）在 patience 耗尽时只把 `self.early_stop=True`（第 39 行），但主循环 `HyperAttentionDTI_main.py:340` 调用后**没有任何 `if early_stopping.early_stop: break`**。因此训练**总是跑满 `hp.Epoch` 个 epoch**，早停只起"保存 best checkpoint"作用，不起"停止训练"作用。
2. **最终测试用的是最后一个 epoch 的模型，而非 best checkpoint**：`valid_best_checkpoint.pth` 被保存（pytorchtools.py:50），但**从未被 `load_state_dict` 加载**（`model.py` 里唯一的 `load_state_dict` 在 main.py:219 已被注释）。`test_model`（main.py:81-95）调用 `test_precess(model, ...)` 用的是内存中 last-epoch 的全局 `model`。故 §7 的 `results.txt` 是 last-epoch 结果，不是 best-validation 结果。这一点与 `预实验记录与代码说明.md` 中"best checkpoint"的描述**不符**。

---

## 6. 评价指标代码对照

位置：`HyperAttentionDTI_main.py:49-79`（`test_precess`）、`:25-43`（`show_result`）。

| 指标 | 论文 | 代码实现 | 一致 |
|---|---|---|---|
| Accuracy | 准确率 | `accuracy_score(Y, P)`，`P=argmax(softmax)`（main.py:65,77） | ✅ |
| Precision | 精确率 | `precision_score(Y, P)`（main.py:72） | ✅ |
| Recall | 召回率 | `recall_score(Y, P)`（main.py:73） | ✅ |
| AUC | ROC-AUC | `roc_auc_score(Y, S)`，`S=softmax[:,1]`（正类概率）（main.py:74） | ✅ |
| AUPR | PR 曲线下面积 | `precision_recall_curve` + `auc(recall, precision)`（main.py:75-76） | ✅（变量命名 tpr/fpr 混乱但数值正确） |
| mean ± std | 均值 ± **标准差** | `np.var()` 输出并**标注为 std**（main.py:26-37） | ❌ 用了方差当标准差 |

> **标准差问题**：`show_result` 用 `np.var()` 计算并打印为 `std`，这是**方差而非标准差**，与论文 Table 5–7 的 `mean ± std` 不符。且代码只跑 **1 个 seed（SEED=1234，main.py:146）**，方差/标准差来自 **5 折之间**，不是多 seed 重复。

---

## 7. 实验设置 E1–E4 的代码对应

### 7.1 当前唯一的数据划分逻辑

- `HyperAttentionDTI_main.py:184`：`shuffle_dataset(train_data_list, SEED)`，`np.random.shuffle` 全量随机打乱。
- `HyperAttentionDTI_main.py:192`：`get_kfold_data(i_fold, dataset)`（第 98-115 行）做 **5 折**划分，80% train / 20% test。
- `HyperAttentionDTI_main.py:196-198`：在 80% train 内再 `random_split` 出 20% valid，即最终 **64% train / 16% valid / 20% test**。
- 划分完全**基于行（drug-protein pair）随机**，**不涉及 drug ID / protein ID 的实体隔离**。
  - 佐证：`collate_fn` 虽然解析出 `drug_id`、`protein_id`（dataset.py:57），但返回时被丢弃（dataset.py:67-68 只返回 `compound_new, protein_new, labels_new`），训练流程中从未用 ID 做划分。

### 7.2 四种实验是否实现

| 实验 | 论文定义 | 代码是否有 | 说明 |
|---|---|---|---|
| E1 | seen drug + seen protein + unseen pair | ⚠️ 等价于随机 pair-level 划分 | 当前 5 折随机划分**语义上即 E1**；且 64/16/20 恰等于论文 DrugBank 的 16:4:5。但无独立 E1 脚本/命名 |
| E2 | novel drug + seen protein（20% drug 作测试） | ❌ | 无按 drug 实体划分的逻辑 |
| E3 | seen drug + novel protein（20% protein 作测试） | ❌ | 无按 protein 实体划分的逻辑 |
| E4 | novel drug + novel protein（20% drug + 20% protein） | ❌ | 无按 drug/protein 实体划分的逻辑 |

### 7.3 random seed

- 唯一 seed：`SEED = 1234`（`HyperAttentionDTI_main.py:146`）。
- `random.seed(SEED)` / `torch.manual_seed(SEED)` / `torch.cuda.manual_seed_all(SEED)`（main.py:147-149）。
- `shuffle_dataset` 里又 `np.random.seed(seed)`（main.py:118）。
- **没有多 seed 循环、没有 seed 作为命令行参数**。

### 7.4 数据泄漏风险

- 对 **E1**：随机 pair-level 划分允许同一 drug/protein 同时出现在 train 和 test（只要 pair 不同），这**符合 E1 定义**，不算泄漏。
- 对 **E2/E3/E4**：由于**根本没有按实体划分**，若有人拿当前随机划分结果去冒充 E2/E3/E4，会存在严重的 **drug/protein 实体泄漏**（测试集实体已在训练集中出现），结果会被高估。这是**必须新增代码**才能解决的地方。

---

## 8. 论文结果（Table 2–8）与代码的对应

| 论文表 | 数据集+设置 | 运行入口 | 参数 | 输入数据 | 输出文件 | 是否能由当前代码生成 |
|---|---|---|---|---|---|---|
| Table 2 | DrugBank+E1 | `python HyperAttentionDTI_main.py`，设 `DATASET="DrugBank"` | 见 §5 | `data/DrugBank.txt` | `DrugBank/results.txt`、`DrugBank/<fold>/...` | ⚠️ 能跑出数，但数值与论文不具可比性（模型/超参/统计均不一致） |
| Table 3 | Davis+E1 | 同上，`DATASET="Davis"` | 同上 | `data/Davis.txt` | `Davis/results.txt` | ⚠️ 同上 |
| Table 4 | KIBA+E1 | 同上，`DATASET="KIBA"` | 同上 | `data/KIBA.txt` | `KIBA/results.txt` | ⚠️ 同上 |
| Table 5 | DrugBank+E2 | **无** | — | — | — | ❌ |
| Table 6 | DrugBank+E3 | **无** | — | — | — | ❌ |
| Table 7 | DrugBank+E4 | **无** | — | — | — | ❌ |
| Table 8 | 消融 | **无**（模型无去 attention 开关） | — | — | — | ❌ |

> 需要强调：即使 Table 2–4，当前代码"能跑"也不等于"能复现论文数值"，原因是 §4/§5 列出的不一致（filter 40 vs 32、protein kernel、LR 5e-5 vs 1e-4、batch、loss、std 用方差、last-epoch 而非 best 等）。

---

## 9. 当前代码缺失 / 不一致清单（汇总）

### 9.1 论文有、但代码没有

1. **E2 / E3 / E4 实验**：无按 drug/protein 实体划分的脚本（`get_kfold_data` 和 `random_split` 均为 pair-level 随机）。
2. **Table 8 注意力消融**：模型没有"关闭 attention"的开关，无消融脚本。
3. **多 seed / 多次独立重复**：只有一个 seed，5 折内取统计。
4. **Davis/KIBA 阈值逻辑**（5.0 / 12.1）：二值化已在离线数据中完成，代码无阈值处理。

### 9.2 代码与论文不一致（数值/实现）

| 项 | 论文 | 代码 | 位置 |
|---|---|---|---|
| CNN filter | 32/64/96 | 40/80/160 | hyperparameter.py:26 `conv=40` |
| protein kernel | 4/6/12 | 4/8/12 | hyperparameter.py:24 |
| Learning rate | 1e-4 | 5e-5 | hyperparameter.py:11 |
| Batch size | 32 | 4（当前） | hyperparameter.py:13 |
| Early stopping | 20 epoch | Patience=50，且不 break | hyperparameter.py:15；main.py:340 |
| Loss | BCE | CrossEntropyLoss(+类别权重) | main.py:227 |
| 标准差 | std | 用方差 `np.var()` | main.py:26-37 |
| 最终测试模型 | best checkpoint | last-epoch（未 load best） | main.py:81-95, 342-345；pytorchtools.py:50 |

### 9.3 需要修改的地方（若要复现）

1. `hyperparameter.py`：改 `conv=32`、`protein_kernel=[4,6,12]`、`Learning_rate=1e-4`、`Batch_size=32`、`Patience=20`。
2. `model.py`：把 Loss 相关改为 BCE（或至少在 main.py 里用 BCE 逻辑）；删除/忽略死参数 `FC_Dropout`。
3. `HyperAttentionDTI_main.py`：
   - 训练循环里加 `if early_stopping.early_stop: break`；
   - 测试前 `model.load_state_dict(torch.load(save_path+'/valid_best_checkpoint.pth'))`；
   - `show_result` 把 `np.var` 换成 `np.std`（或另列方差）；
   - 新增 `--dataset`、`--seed` 等命令行参数。
4. **新增脚本**（复现 E2–E4 必需）：按 drug/protein 实体划分数据集：
   - E2：随机 20% **drug** 相关的全部 pair 作 test，其余作 train/valid；
   - E3：随机 20% **protein** 相关的全部 pair 作 test；
   - E4：随机 20% drug + 20% protein，其组合 pair 作 test；
   - 每个设置用多个 seed 重复取 mean ± std。
5. **新增消融脚本**（复现 Table 8）：在 `model.py` 加一个"是否启用 HyperAttention"的开关（或去掉加权分支），对比有无 attention 的结果。

---

## 10. 最终复现路线（从环境到 Table 2–8）

> 以下步骤为"需要做哪些事"，**不包含先修改代码之外的改动**（本报告只做静态分析；实际改动需另行执行）。

### 阶段 A：环境配置

1. 准备 NVIDIA GPU 机器（代码硬编码 `.cuda()`，`main.py` 多处 `xxx.cuda()`，无 GPU 无法运行）。
2. 按 `HpyerAttentionDTI/Linux安装说明.md` 或 `requirements.txt` 安装：`torch`(CUDA 版)、`numpy`、`scikit-learn`、`tqdm`、`tensorboardX`、`prefetch_generator`。
3. 验证 `python -c "import torch; print(torch.cuda.is_available())"` 输出 `True`。

### 阶段 B：先修正参数与流程（见 §9.3）

- 把 filter、kernel、lr、batch、patience、loss、early-stop-break、best-checkpoint 加载、std 算法逐项对齐论文。

### 阶段 C：复现 Table 2–4（E1，三个数据集）

1. 分别设置 `DATASET="DrugBank"/"Davis"/"KIBA"`，`K_Fold=5`，运行 `python HyperAttentionDTI_main.py`。
2. 从 `DrugBank/results.txt`、`Davis/results.txt`、`KIBA/results.txt` 读取 AUC/AUPR/Acc/Prec/Recall 均值。

### 阶段 D：复现 Table 5–7（DrugBank，E2/E3/E4）—— 需先新增脚本

1. 新增实体划分脚本（如 `run_E2.py` / `run_E3.py` / `run_E4.py`），按 §9.3 第 4 条实现 drug/protein-level split。
2. 多 seed 重复（如 5–10 个 seed），统计 `mean ± std`（真标准差）。

### 阶段 E：复现 Table 8（消融）

1. 新增消融脚本 + 模型开关，分别训练"有 attention / 无 attention"两个变体，对比指标。

### 结论

- **能直接跑通**：模型训练/验证/测试全链路（入口 `python HyperAttentionDTI_main.py`）。
- **不能直接得到论文数值**：因 §9.2 的参数/统计/流程不一致。
- **完全缺失、必须补写代码**：E2/E3/E4 的实体划分、Table 8 消融、多 seed 统计。
